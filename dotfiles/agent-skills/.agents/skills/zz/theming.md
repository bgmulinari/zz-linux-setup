# Theming: Themes, Wallpaper, Accent, Icons, Fonts

Read this before changing colors, themes, wallpaper, icon theme, cursor, or fonts.

DMS is the single source of the look. It picks a theme (a registry theme or a
wallpaper-derived Material palette), renders it through matugen into every
application ZZ wires up, and keeps them in sync on each change. The shipped default is
the Catppuccin registry theme: mocha with the blue accent in dark mode, latte with the
blue accent in light mode, Yaru-blue icons for GTK and the shell, Breeze Dark icons
for Qt/KDE apps, JetBrainsMono Nerd Font.

**Commands:** `dms ipc call theme dark|light|toggle`, `dms ipc call wallpaper set <path>`,
`dms ipc call wallpaper next|prev`, Settings > Theme / Wallpaper / Appearance

## What follows the palette automatically

| Target | Generated file | Personal escape hatch |
|--------|----------------|-----------------------|
| Niri colors | `~/.config/niri/dms/colors.kdl` | none; colors are DMS-owned |
| Ghostty | `~/.config/ghostty/themes/dankcolors` | set `theme = <other>` in `~/.config/ghostty/local` |
| btop | `~/.config/btop/themes/dank.theme` | pick another `color_theme` in `btop.conf` |
| Qt apps | `~/.local/share/color-schemes/DankMatugen.colors` via qt6ct/kdeglobals | change the scheme in qt6ct |
| GTK/libadwaita | `dank-colors.css` imported from `~/.config/gtk-3.0/gtk.css`, `gtk-4.0/gtk.css` | remove the import line |
| Starship | `[palettes.zz]` block in `~/.config/starship.toml` | delete the marker comments to opt out |
| Icon theme (GTK, shell) | Yaru variant nearest the accent, applied to gsettings and DMS | set another icon theme in Settings > Theme |
| Icon theme (Qt/KDE) | Breeze Dark in `~/.config/qt6ct/qt6ct.conf` and `kdeglobals`; not synced | change it in qt6ct |
| VS Code | theme `Dynamic Base16 DankShell` (extension `danklinux.dms-theme`) | choose another theme in VS Code |
| Zed | `DankShell Dark` / `DankShell Light` | choose another theme in Zed |
| Firefox | Pywalfox, when the optional theme unit was installed | disable the extension |

If a target stops following the palette, the escape hatch was probably taken; check
it before debugging DMS.

## Changing the theme

- Registry themes: Settings > Theme lists installed ones; install more from the DMS
  registry there. A registry theme is one `theme.json` under
  `~/.config/DankMaterialShell/themes/<id>/`. The shipped catppuccin file is a product
  link; copy it to a new id to customize
- Wallpaper-derived: pick "Dynamic"/auto in Settings > Theme and the palette follows
  the wallpaper (`dms ipc call wallpaper set <path>` also drives the accent then)
- Mode: `dms ipc call theme toggle`; scheduled light/dark is in Settings > Theme
- Accent, corner radius, transparency, blur, bar elevation, animation speed: Settings >
  Appearance. ZZ ships `cornerRadius 0`, popup transparency 0.9, blur on

## Fonts

`fontFamily` and `monoFontFamily` are DMS settings (Settings > Appearance); ZZ seeds
`JetBrainsMono Nerd Font` for both. Ghostty's font is separate: `font-family` in
`~/.config/ghostty/local`.

## Adding your own matugen template

DMS appends every `*.toml` under `~/.config/matugen/dms/configs/` to its matugen run.
The ZZ drop-ins there (`zz-icon-theme.toml`, `zz-starship.toml`, `zz-btop.toml`) are
product links; add a personal file beside them to render another template on every
theme change:

```toml
[templates.myapp]
input_path = "~/.config/matugen/templates/myapp.tpl"
output_path = "~/.config/myapp/colors.conf"
post_hook = "myapp --reload"
```

Templates use the matugen variable syntax; copy one of the ZZ templates under
`~/.zz/dotfiles/dms/.config/matugen/templates/` as a starting point.

## Greeter

The login screen (`dms-greeter`) follows the user's theme through cache symlinks and
a per-user preview generated by `dms-greeter sync --profile` at first login. If the
greeter looks stale after a theme change, run `dms-greeter sync --profile` as the user.
