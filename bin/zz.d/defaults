#!/usr/bin/env bash
# zz-name: defaults
# zz-summary: Reapply default applications and browser preferences.
# zz-usage: zz defaults
set -Eeuo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
exec "$ROOT_DIR/install.sh" defaults "$@"
