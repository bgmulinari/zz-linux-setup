#!/usr/bin/env bash
# zz-name: doctor
# zz-summary: Run desktop readiness and post-install checks.
# zz-usage: zz doctor [options]
set -Eeuo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
exec "$ROOT_DIR/install.sh" doctor "$@"
