#!/usr/bin/env bash
# zz-name: first-run
# zz-summary: Run idempotent first-login desktop setup.
# zz-usage: zz first-run
set -Eeuo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
exec "$ROOT_DIR/install.sh" first-run "$@"
