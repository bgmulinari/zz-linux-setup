#!/usr/bin/env bash
# zz-name: wizard
# zz-summary: Run the interactive setup wizard.
# zz-usage: zz wizard [install options]
set -Eeuo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
exec "$ROOT_DIR/install.sh" wizard "$@"
