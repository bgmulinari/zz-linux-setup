#!/usr/bin/env bash
# zz-name: install
# zz-summary: Run setup from defaults, saved selections, or flags.
# zz-usage: zz install [install options]
set -Eeuo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
exec "$ROOT_DIR/install.sh" install "$@"
